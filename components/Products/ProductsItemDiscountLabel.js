const ProductItemDiscountLabel = ({ discount }) => (
    <div className="z-10 products-item-discount bg-primary font-semibold text-sm">
        -{discount}%
    </div>
);

export default ProductItemDiscountLabel;